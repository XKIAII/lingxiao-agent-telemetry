{
  "name": "agent-core-demo",
  "version": "1.0.0",
  "description": "Claude Code 5-Layer Agent Architecture Demo — 记忆层 / 知识层 / 护栏层 / 委派层 / 分发层",
  "main": "src/demo.ts",
  "scripts": {
    "start": "npx tsx src/demo-server.ts",
    "demo": "npx tsx src/demo.ts",
    "status": "node bin/agent-ctl.mjs status",
    "hooks": "node bin/agent-ctl.mjs hooks",
    "logs": "node bin/agent-ctl.mjs logs",
    "test": "echo \"No tests yet\""
  },
  "type": "module",
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "express": "^5.2.1"
  }
}
